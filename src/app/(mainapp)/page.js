import Image from "next/image";

export default function Home() {
  return <>
    <h1 className="m-8">google auth is added(signin with google), Mongodb is added we will be using mongoose to interact with mongoDb to perform crud operations accordingly</h1>
  </>
}
